$(document).ready(function () {
  $(".slider").slick({
    // autoplay: true,
    // centerMode: true,
    infinite: true,
    slidesToShow: 1,
    speed: 700,
    slidesToScroll: 1,
    // adaptiveHeight: true,
  });
});
